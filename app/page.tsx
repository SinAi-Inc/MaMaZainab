// Re-export the coming-soon page as the root landing page
// so "/" renders the landing directly (no redirect).
export { default } from "./coming-soon/page";
export { metadata } from "./coming-soon/page";

